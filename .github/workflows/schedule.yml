name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ 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 }}
          GIST_ID: ${{ fdd7d0e1e1eb9bd7130a07157b9534df }}
          TIMEZONE: Asia/Seoul
